hook() {
        last_cmd=$(history 1 | sed "s/ *[0-9]* *//")
        if bash -n -c "$last_cmd" 2>/dev/null; then
                ~/Command-Achievements/run "$last_cmd" $USER
                fi
}
PROMPT_COMMAND=hook